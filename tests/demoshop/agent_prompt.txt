Use the connected RobotMCP MCP server (6 tools: rf_session [action=open|get|close; on get, pass since_version=N to skip the full reply when nothing changed], rf_execute_step, rf_context [action=get|set], rf_manage_session [imports + declarative settings/variables/tags], rf_export_suite [renders the recorded session into a canonical RF7 .robot suite — file-first manifest by default], app_inspect_state [snapshot_kind=app_context|dom|dom_selector|aria|screenshot|console_log|network_log; selector, return_inline, summary_only, include_shadow_dom as needed]) to create a Robot Framework test suite and execute it STEP BY STEP against a real browser, then build the final suite.

Scenario — target site https://demoshop.makrocode.de/ (a shop called "Flowline Supply"):
- Open https://demoshop.makrocode.de/
- Add an item to the cart
- Assert the item was added to the cart
- Add another item to the cart
- Assert the second item was added to the cart
- Checkout
- Assert checkout was successful

How to work:
1. Open a live session with rf_session(action="open"). Capture the returned session_id.
2. Run setup as ONE batched call: rf_execute_step(session_id, instructions=[...]) — this returns the session summary once instead of once per step. Begin with these setup steps (in order):
   - Import Library    Browser
   - New Browser    chromium    headless=True
   - Set Browser Timeout    60s
   - New Context
   - New Page
   - Go To    https://demoshop.makrocode.de/    wait_until=domcontentloaded
   Do NOT use "New Page <url>" — the page "load" event can hang; always navigate with "Go To <url>    wait_until=domcontentloaded".
3. You do NOT know this site's selectors. Use app_inspect_state(session_id, snapshot_kind="aria") for structural inspection — Playwright walks Shadow DOM and iframes automatically and the YAML is far cheaper than raw HTML. Fall back to snapshot_kind="dom" (or "dom_selector" with a selector) when ARIA isn't enough. The response carries `manifest.path` (read the file when you need the full payload) and `manifest.summary` (use this to decide). Pass `return_inline=True` only when you really need the bytes in-band. If a step returns ok=false (step-failed), capture a fresh aria/dom snapshot, correct the selector, and retry — this iteration is expected and is the point of stepwise authoring.
4. Prefer Browser Library inline assertions, e.g. "Get Text    <selector>    ==    <value>" or "Get Element Count    <selector>    ==    2", and "Get Url    *=    <fragment>".
5. When all scenario steps pass live, BUILD the final suite via rf_export_suite(session_id=..., target_path="tests/demoshop/demoshop_checkout_authored.robot", test_case_name="Demoshop Checkout"). The tool renders canonical RF7 from the recorded steps and persists the file; the response carries a manifest with path/bytes/sha256. (You no longer need to write the file by hand.)
6. Prove it runnable with Bash: uv run --group web python -m robot --outputdir tests/demoshop/results tests/demoshop/demoshop_checkout_authored.robot
7. Close the session with rf_session(action="close", session_id=...).

Keep going until the suite is built AND the standalone robot run passes; do not stop early. If the live site is intermittently slow, raise the Browser timeout and retry rather than giving up.

Finally print a concise SUMMARY containing: total live steps executed, how many steps failed and were corrected (and the specific selector/issue for each), the final suite path, and whether the standalone `robot` run passed.
